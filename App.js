import React, { Component } from 'react';
import { View, Image } from 'react-native';
import NavigationComponent from './src/core/NavigationComponent';

export default class App extends Component {
  render() {
    return (
      <NavigationComponent/>
    );
  }
}